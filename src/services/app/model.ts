export interface AppModel {
  row: string;
  first: string;
  second: string;
}
export interface AppResponseModel<T> {
  data: T;
}
export interface AppQueryInputModel {
  first: string;
  second: string;
}
export interface AppQueryOutputModel {
  row: string;
  first: string;
  second: string;
}
