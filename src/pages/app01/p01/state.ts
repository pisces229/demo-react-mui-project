export interface FormState {
  first: string;
  second: string;
}
export const initialFormState: FormState = {
  first: '',
  second: '',
}
export interface GridState {
  checkbox: boolean;
  row: string;
  first: string;
  second: string;
}
