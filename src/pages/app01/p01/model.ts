export interface FormModel {
  first: string;
  second: string;
}
export const initialFormModel: FormModel = {
  first: '',
  second: '',
}
export interface GridModel {
  check: boolean;
  first: string;
  second: string;
}
